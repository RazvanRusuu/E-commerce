import React from "react";
import PageHero from "../components/PageHero";

const CheckoutPage = () => {
  return (
    <main>
      <PageHero title="Checkout" />
      <section className="section-checkout container padding-side">
        <h2 className="heading-secondary">Checkout Here</h2>
      </section>
    </main>
  );
};

export default CheckoutPage;
